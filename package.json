{
  "name": "bedrock-kb-chatbot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "infra"
  ],
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "format:app": "cd app && yarn format",
    "format:infra": "cd infra && yarn format",
    "lint:app": "cd app && yarn lint",
    "lint:infra": "cd infra && yarn lint"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5"
  }
}
