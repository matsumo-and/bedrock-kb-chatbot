# Confluence Configuration
# Confluence host URL (e.g., https://your-domain.atlassian.net)
CONFLUENCE_HOST_URL=

# Comma-separated list of Confluence spaces to index (e.g., TECH,DOC,KB)
# Use "*" to include all spaces
CONFLUENCE_SPACES=*

# Note: The Confluence credentials (username and API token) will be stored
# in AWS Secrets Manager. After deploying the stack, update the secret using:
# aws secretsmanager update-secret --secret-id <secret-arn> --secret-string '{"username":"your-email@example.com","password":"your-confluence-api-token"}'
confluenceAppKey: アプリキー
confluenceAppSecret: アプリシークレット
confluenceAccessToken: アプリアクセストークン
confluenceRefreshToken: アプリ更新トークン